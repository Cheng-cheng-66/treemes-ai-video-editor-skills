# Changelog

## 0.10.0-beta.4 - 2026-08-14

### Fixed

- 修复公开包将工厂实拍错误标记为 `planned + disabled` 的问题；该限制不是用户
  决策，也不再阻止工厂实拍执行。

### Added

- 增加可调用的 `skills.factory_shoot.runner:run` 和 `scripts/run_factory_shoot.py`。
- 增加工厂实拍计划合同、A/B/C/D同步区校验、动作锚点保护、真实源哈希验证、
  源画幅继承、固定标题与单行字幕宽度检测。
- 增加 `picture_master_no_audio.mp4`、`dialogue_raw.wav`、`ambience.wav`、
  `bgm.wav`、开放工具预览、剪映四轨导入清单和完整自动QC/人工听审模板。
- 增加工厂实拍合成端到端渲染、完整解码和未审核字段 `null` 保护测试。

### Boundary

- 启用表示能够实际生成可播放Beta候选和证据，不表示剪映原生降噪已经执行。
- 字幕逐句匹配、专业词、口型、动作、故事完整性、BGM听感和最终观感仍需人工
  审核，自动程序不得把这些字段从 `null` 改成通过。

## 0.10.0-beta.3 - 2026-08-14

### Added

- 增加 macOS 双击安装器 `安装.command`，普通用户不再需要复制 Codex
  安装提示词或手工输入终端命令。
- 增加只从 Git 跟踪文件生成的单顶层目录 ZIP 安装包和 SHA-256 校验文件。
- 增加下载包结构、执行权限、校验值和隔离 `CODEX_HOME` 安装测试。

### Fixed

- 忽略 macOS 外置盘上的 `._*.py` AppleDouble 元数据旁车文件，避免安装后
  doctor 将二进制元数据误当成 Python 源码。

### Safety

- 更新时不直接删除旧 Skill，而是保留时间戳备份。
- 安装器拒绝覆盖符号链接目标，并先在同一目录的临时区完成复制。
- 不包含客户素材、成片、模型、剪映账号、会员权益或受许可限制的 BGM。

## 0.10.0-beta.2 - 2026-08-14

### Fixed

- 将仓库根目录正式定义为单一可安装 Codex Skill，避免分别安装场景子目录后缺失
  `core`、`presets`、`scripts` 和 `schemas`。
- 增加 Codex 一键安装提示、首次运行自检规则和 UI 元数据。
- 发布检查新增根 Skill、共享运行库和安装入口校验。

### Boundary

- 工厂实拍的无人值守生产入口仍保持禁用。
- 客户案例的模型、客户事实、隐私和最终导演验收仍是显式门禁。
- 本修复不包含素材、模型、剪映账号、会员权益或受许可限制的 BGM。

## 0.10.0-beta.1 - 2026-07-31

### Added

- 视频日记源画幅继承：横屏源默认输出 16:9，竖屏源默认输出 9:16。
- 视频日记剪映音频固定规格：仅保留人声、+10 dB、固定 BGM -8 dB。
- 工厂实拍混合路线预设、四轨资产契约、C_AUDIO_FREE 实验和剪映模板。
- 客户案例分析工作流、同步区、停顿、故事规划、质量门禁和测试。
- 完整 Mac 移植包、增量更新、回滚与新电脑检查流程。

### Changed

- 工厂实拍字幕默认改为 96px、4px 字间距、7px 黑描边、基线 1280。
- 长字幕必须按真实语义和人声时间拆分，禁止自动缩小或改写原话。

### Release boundary

- 本版本为 Beta，不创建 stable 标签。
- 原素材、成片、音频、剪映草稿、账号、登录态、密钥和本机配置不进入 Git。
- 未人工听审的字幕、人声、口型和观感字段继续保持 `null` 或 `NOT_REVIEWED`。

## 0.9.0-beta.1 - 2026-07-25

### Added

- Python 配置、路径、Skill 注册、QC 和 Release 共用底座。
- 视频日记独立 Skill 与 V5 兼容渲染入口。
- 工厂实拍、客户案例禁用边界。
- bootstrap、doctor、合成技术样片、回归、显式更新和回滚命令。
- stable/beta 通道策略、迁移和生产验收文档。

### Compatibility

- macOS V5 封面与正文顶部模板生成结果与迁移前 SHA-256 一致。
- 既有成片、原素材、历史报告未修改或删除。

### Known blockers

- 标准原素材未挂载，无法执行全量重渲染。
- 复杂样片与异常样片未提供。
- V5 的逐字、人声、音画和标题状态仍需真实样片人工复核。
- 因此本版本不允许进入 stable 或生产电脑。
