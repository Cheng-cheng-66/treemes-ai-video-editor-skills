# Schema index

The current Beta validates the edit-plan and caption contracts in
`skills.video_diary.runner`. A standalone JSON Schema has not yet replaced these runtime
validators. Treat that migration as future work, not an existing capability.
