# video_diary

Enabled Beta workflow for recurring branded video diaries. The executable entrypoint is declared
in `skill.json`. `SKILL.md` is the agent procedure; canonical presets remain at
`presets/video_diary/` to preserve the accepted project layout.

Human listening, subtitle fidelity, visual comfort and Jianying audio acceptance remain required
for every export.
