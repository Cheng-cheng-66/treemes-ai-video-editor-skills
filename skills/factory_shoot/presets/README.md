# Preset index

- `presets/factory_demo/`: title and subtitle rules extracted from reference work.
- `presets/factory_demo_hybrid/`: editorial, sync, title, subtitle, Jianying audio, mix and quality contracts.

These remain root-level canonical presets to avoid a second migration.
