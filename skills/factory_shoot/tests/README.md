# Test index

`tests/test_factory_demo_hybrid.py` verifies portable preset contracts. Evidence tests requiring
real local runs must skip when that private evidence is absent; a skip is not production proof.
